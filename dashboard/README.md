# Next.js Dashboard Admin

This is a Next.js dashboard application for admin management.

## Getting Started

1. **Install dependencies:**
   ```bash
   npm install
   ```

2. **Run the development server:**
   ```bash
   npm run dev
   ```

3. **Open your browser:**
   Navigate to [http://localhost:3000](http://localhost:3000) to see the application.

## Project Structure

- `src/app/` - Next.js app directory with pages and layouts
- `src/app/page.tsx` - Home page
- `src/app/layout.tsx` - Root layout component
- `src/app/globals.css` - Global styles with Tailwind CSS

## Features

- ⚡ Next.js 16 with App Router
- 🎨 Tailwind CSS for styling
- 📘 TypeScript support
- 🔍 ESLint for code quality

## Available Scripts

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run start` - Start production server
- `npm run lint` - Run ESLint

## Configuration

The project uses:
- **TypeScript** for type safety
- **Tailwind CSS** for styling
- **ESLint** for linting
- **App Router** for routing (Next.js 13+ feature)

## Next Steps

- Create admin pages in `src/app/`
- Add API integration to connect with the Laravel backend
- Implement authentication
- Add admin dashboard components
